# Render Blueprint - Generated by InoPay NCS 2.0
services:
  - type: web
    name: gomonto-frontend
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /
    autoDeploy: false
    scaling:
      minInstances: 1
      maxInstances: 3

  - type: web
    name: gomonto-api
    env: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    healthCheckPath: /health
    autoDeploy: false

databases:
  - name: gomonto-db
    plan: starter
    databaseName: gomonto_db
