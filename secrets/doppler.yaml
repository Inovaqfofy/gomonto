# Doppler Configuration - gomonto
# Generated by InoPay NCS 2.0

setup:
  project: gomonto
  config: prd

# Commands:
# doppler setup
# doppler run -- npm start
# doppler secrets download --no-file --format env > .env
